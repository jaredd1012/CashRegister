# Optional: API base URL for the change calculator (default: http://localhost:3001)
NEXT_PUBLIC_API_URL=http://localhost:3001

# PostgreSQL connection string (required for storing transaction history)
# Put in .env at project root or app/.env — the server loads from both
# Example: postgresql://youruser:yourpassword@localhost:5432/cash_register
DATABASE_URL=postgresql://jareddavis@localhost:5432/cash_register
